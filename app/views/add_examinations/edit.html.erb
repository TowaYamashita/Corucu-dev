<h1>
  <%= t('activerecord.models.add_examination') + "情報を編集" %>
</h1>
<%= render 'form', add_examination: @add_examination %>

<%= link_to 'Show', @add_examination %> |
<%= link_to 'Back', add_examinations_path %>
